/*
void copyArr(unsigned int n, unsigned int m, int** copy, int** origin){
    for(int row = 0; row < n; row ++){
        for(int col = 0; col < m; col++){
            *(*(copy + row) + col) = *(*(origin + row) + col);
        }
    }
}
*/
